<p id="notice"><%= notice %></p>

<h1>
 
  <%= @beer_club.name %>
</h1>

<p>
  <strong>Founded:</strong>
  <%= @beer_club.founded %>
</p>

<p>
  <strong>City:</strong>
  <%= @beer_club.city %>
</p>


<h2>
  Members:
 
</h2>

<p>

  <% if @beer_club.memberships.empty? %>
	Beer Club has not yet any members!
  <% else %>
	<% @beer_club.users.each do |user| %>
		
		
		<ul><%= link_to user.username, user %></ul>
	<% end %>
  <% end %>
</p>

<% if current_user %>

	<% if @beer_club.users.find{ |u| u == current_user }.not_nil? %>

		<h4>Join the Beer club: </h4>
		<%= form_for(@membership) do |f| %>

			<%= f.hidden_field :beer_club_id %>
			<%= f.submit value:"join the club" %>
		<% end %>

	<br>
	<% end %>

	
<% end %>

<%= link_to 'Edit', edit_beer_club_path(@beer_club) if current_user %> 

